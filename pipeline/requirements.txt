# Pipeline 1 — Raw Ingestion dependencies.
#
# mgz==1.8.51 is assumed already installed (do not upgrade) and is intentionally
# NOT pinned here so this file does not trigger a reinstall.
#
# chromadb bundles its default embedding function (all-MiniLM-L6-v2), so no API
# key or extra model download configuration is required.
chromadb

# Pipeline 2 — Telemetry & Timelines dependencies.
#
# boto3 talks to DynamoDB (local via DynamoDB Local, or real AWS — see
# pipeline/config.py). requests calls the local Ollama HTTP API directly
# (no ollama pkg needed); already pulled in transitively but listed
# explicitly since pipeline2 depends on it directly.
boto3
requests

# Pipeline 3 — Player Profiling dependencies.
#
# Reuses boto3 (now also talks to MinIO/S3 — see pipeline/s3_store.py) and
# requests (calls Ollama's /api/generate directly, same as pipeline 2). No
# new packages required.
